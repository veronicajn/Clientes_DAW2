//==================ACTIVIDAD 5================================================================
function unircadenas(numeros){

   let resultado = numeros.join(',');
   console.log( resultado );
}
let array = [10, 6, 32, 52, 13, 8];
unircadenas(array);